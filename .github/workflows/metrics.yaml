name: Metrics
on:
  # (optional) Run workflow manually
  workflow_dispatch:
  # Schedule daily updates: 0 0 * * *
  schedule:
    - cron: "* * * * *"
  push: { branches: ["main", "master"] }

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: metrics.plugin.languages.details.svg
          token:
            ${{ secrets.METRICS_TOKEN }}
            # GITHUB_TOKEN is a special auto-generated token used for commits
          committer_token: ${{ secrets.GITHUB_TOKEN }}
          committer_branch: metrics
          committer_message: "update `${filename}`"

          # base: ""
          plugin_languages: yes
          plugin_languages_indepth: yes
          plugin_languages_ignored: html, css, Procfile
          plugin_languages_skipped: lowlighter/metrics
          plugin_languages_details: lines, bytes-size, percentage
          plugin_languages_limit: 10
          plugin_languages_aliases: tsx:typescript jsx:javascript
          commits_authoring: firstname lastname, username, SirHarveyBix@users.noreply.github.com
